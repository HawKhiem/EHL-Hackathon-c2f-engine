# --- env / secrets ---
.env
.env.local
.env.*.local
!.env.example

# --- node ---
node_modules/
dist/
dist-ssr/
*.local
.vite/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*

# --- python ---
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- supabase (local stack) ---
supabase/.branches/
supabase/.temp/
supabase/.env

# --- editors / os ---
.DS_Store
Thumbs.db
.idea/
*.swp

# --- agent tooling ---
.codegraph/
.claude/settings.local.json

# --- entire ---
.claude
.codex

# --- challenge case data (local only, never commit) ---
cases/

# --- claim data: NEVER check in (hackathon rule). The whole runs/ tree stays on disk only:
# run logs embed each case's policy/description/invoice text, and the pricing engine reads
# them as its market memory - but none of it belongs in the repository.
cases/
runs/
